import type { W3DocsConfig } from '@w3docs/ui'

const config: W3DocsConfig = {
  title: "{{title}}",
  description: "{{description}}",
  chain: {{chain}},
  address: "{{address}}",
  verified: {{verified}}
}

export default config
